const fix=document.createElement('style');fix.textContent='@media(max-width:850px){.portal h3{font-size:clamp(3.5rem,15vw,7rem)}.portal-sierra h3{font-size:clamp(2.9rem,12vw,5.7rem)}.portal-meta span:last-child{text-align:right}}';document.head.appendChild(fix);
const cursor=document.querySelector('.cursor');if(matchMedia('(pointer:fine)').matches){addEventListener('pointermove',e=>{cursor.style.left=`${e.clientX}px`;cursor.style.top=`${e.clientY}px`});document.querySelectorAll('a').forEach(a=>{a.addEventListener('pointerenter',()=>{cursor.style.width='42px';cursor.style.height='42px'});a.addEventListener('pointerleave',()=>{cursor.style.width='12px';cursor.style.height='12px'})})}const reveals=document.querySelectorAll('.reveal');requestAnimationFrame(()=>reveals.forEach((el,i)=>setTimeout(()=>el.classList.add('is-visible'),120+i*130)));
